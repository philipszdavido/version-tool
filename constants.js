const VCS = ".vcs"
const vcsRegistry = "registry.json"
const HEAD = "HEAD"
const BRANCHES = "branches"
const STAGE = "stage"
const VCSIGNORE = ".vcsignore"

const vcsConstants = {
    VCS,
    HEAD,
    BRANCHES,
    STAGE,
    VCSIGNORE
}

module.exports = vcsConstants